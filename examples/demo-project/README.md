# CodeMesh demo project

Open this folder in VS Code or Cursor, then ask CodeMesh:

> The UserService crashes when the API returns an empty response. Fix the bug and enable/add a test.

The project is intentionally small and imperfect so the Explorer → Coder → Reviewer path is easy to demonstrate.
